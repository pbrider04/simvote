fastapi==0.111.0
uvicorn==0.30.1
jinja2==3.1.2
# sqlite3 is a built-in module in Python, so it's not installed via pip.
# However, if you were using a Python wrapper for sqlite3 like pysqlite3, you would specify it here.
# For standard library sqlite3, no entry is needed.
